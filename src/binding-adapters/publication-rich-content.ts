import { createWebsiteBuilderTiptapJsonBindingAdapter } from "@init-modules/website-builder";

export const publicationRichContentBindingAdapter =
  createWebsiteBuilderTiptapJsonBindingAdapter(
    "publication-website-builder::rich-content-json",
  );
